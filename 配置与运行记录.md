# PiliPlus Windows 端 —— 配置、构建与打包记录

> 记录时间：2026-09-13
> 项目路径：`D:\Downloads\Download\2\2\PiliPlus`
> 目标平台：Windows 桌面（x64）
> 结论：已完成配置，Debug / Release 均可正常构建运行，并已产出安装包与便携包。

---

## 1. 环境信息

| 项目 | 值 |
| --- | --- |
| Flutter | 3.47.4 stable（`D:\Source\flutter`，revision `9584c6713b`） |
| Dart | 3.13.3 |
| 项目要求 | `pubspec.yaml` 原始声明 `flutter: 3.47.3`（精确版本） |
| pub 镜像 | `pub.flutter-io.cn`（pub 缓存位于 `%LOCALAPPDATA%\Pub\Cache\hosted`） |
| C++ 工具链 | Visual Studio 2026（MSVC），`flutter doctor` 检查通过 |
| NuGet | 本次安装到 `D:\Tools\nuget\nuget.exe`（Flutter Windows 插件依赖所必需） |
| Inno Setup | 6（`C:\Program Files (x86)\Inno Setup 6`，中文语言文件已存在） |
| fastforge | 0.6.12（`dart pub global activate fastforge`，可执行文件在 `%LOCALAPPDATA%\Pub\Cache\bin`） |
| 操作系统 | Windows（win32 / cmd.exe） |

---

## 2. 配置过程（按实际处理顺序）

### 2.1 放宽 Flutter SDK 版本约束

**问题**：`pubspec.yaml` 中写的是精确版本 `flutter: 3.47.3`，而本机是 3.47.4，导致 `flutter pub get` 直接失败。

**处理**：改为下限约束。

```yaml
environment:
  sdk: ">=3.13.0"
  flutter: ">=3.47.3"      # 原为 flutter: 3.47.3
```

**说明**：3.47.4 与 3.47.3 差异仅一个补丁版本，项目自带的补丁可干净应用（见 2.3），因此无需降级 SDK。

---

### 2.2 安装 NuGet

**问题**：`flutter build windows` 报错，`flutter_inappwebview_windows` 插件需要通过 NuGet 拉取 WebView2 / WIL 等原生依赖，而系统未安装 NuGet CLI。

**处理**：下载 NuGet CLI 到工作区外目录（构建时临时加入 PATH）。

```powershell
New-Item -ItemType Directory -Force -Path "D:\Tools\nuget" | Out-Null
Invoke-WebRequest -Uri "https://dist.nuget.org/win-x86-commandline/latest/nuget.exe" `
  -OutFile "D:\Tools\nuget\nuget.exe"
```

> 后续所有 Windows 构建命令都需要先执行：
> `set PATH=D:\Tools\nuget;%PATH%`

---

### 2.3 给 Flutter SDK 与 material_ui 打补丁（关键步骤）

**问题**：项目代码直接引用了 Flutter 框架的内部实现（如 `TabBarState`、`StandardBottomSheet`、`RenderParagraphEllipsis` 等），未打补丁时 Dart 编译报错。

**依据**：项目自带补丁与脚本，CI 流程见 `.github/workflows/win_x64.yml`：

```powershell
lib/scripts/build.ps1           # 生成版本定义文件
lib/scripts/patch.ps1 windows   # 应用补丁
```

**处理**：按 `patch.ps1` 的逻辑手动应用（本机为 PowerShell，未直接执行脚本以精确控制过程）。

1. **框架补丁 → Flutter SDK**

   在 `D:\Source\flutter` 下依次 `git apply` 以下 21 个补丁（来自 `lib/scripts/*.patch`）：

   ```
   modal_barrier            text_selection           mouse_cursor
   image_anim               layout_builder           navigation_drawer
   popup_menu               fab                      null_safety_for_selectable_region
   selectable_region        editable_text            text_field
   scroll_position          scrollable               scrollable_gesture
   draggable_scrollable_sheet  scaffold              text
   text_painter             sliver                   refresh_indicator
   ```

   应用前已用 `git apply --check` 全部验证可干净应用，最终 SDK 共 **37 个文件**被修改。

2. **材质补丁 → material_ui 包**

   目标：`%LOCALAPPDATA%\Pub\Cache\hosted\pub.flutter-io.cn\material_ui-1.2.0`
   补丁来自 `lib/scripts/material/*.patch`，共 8 个：

   ```
   modal_barrier_material   navigation_drawer   popup_menu   fab
   text_field               scaffold            refresh_indicator   tabs
   ```

   **关键坑**：仓库中的补丁文件是 **CRLF** 换行，直接 `git apply` 会失败；`patch.ps1` 内部会先转成 LF。处理方式是把补丁读入内存、`\r\n` → `\n` 后写入临时文件再应用。

   > Windows 平台无 cupertino 补丁。

**影响范围（重要）**：补丁修改的是**全局 Flutter SDK** 与 pub 缓存，属于不可逆改动。

**还原方式**：

```powershell
# 还原 Flutter SDK
git -C D:\Source\flutter checkout -- .

# 还原 material_ui：删除缓存目录后重新 pub get
# 删除 %LOCALAPPDATA%\Pub\Cache\hosted\pub.flutter-io.cn\material_ui-1.2.0
```

---

### 2.4 获取 mpv / ANGLE 原生依赖（GitHub 直连失败）

**问题**：`media_kit_libs_windows_video` 插件在 CMake 配置阶段需要从 GitHub Releases 下载两个压缩包，直连 GitHub 超时，导致下载出 **0 字节文件**，MD5 校验失败：

```
Integrity check failed, please try to re-build project again.
```

**处理**：通过 `ghfast.top` 代理预下载到 CMake 期望的位置 `build\windows\x64\`，并手工核对 MD5（与插件 `CMakeLists.txt` 中声明的一致）：

| 文件 | 大小 | 期望 MD5 | 校验 |
| --- | --- | --- | --- |
| `mpv-dev-x86_64-20260607-git-43b14a4.7z` | 10,901,340 B | `b84900bbc6fcb995ca6a24f62bee671f` | 通过 |
| `ANGLE.7z` | 5,035,414 B | `e866f13e8d552348058afaafe869b1ed` | 通过 |

```powershell
$dst = 'build\windows\x64'
curl.exe -sL -o "$dst\mpv-dev-x86_64-20260607-git-43b14a4.7z" `
  "https://ghfast.top/https://github.com/bggRGjQaUbCoE/mpv-winbuild-cmake/releases/download/20260607/mpv-dev-x86_64-20260607-git-43b14a4.7z"
curl.exe -sL -o "$dst\ANGLE.7z" `
  "https://ghfast.top/https://github.com/alexmercerind/flutter-windows-ANGLE-OpenGL-ES/releases/download/v1.0.1/ANGLE.7z"
```

> 插件逻辑：若目标文件已存在且 MD5 匹配，则跳过下载。因此**构建前把这两个文件放到 `build\windows\x64\` 即可绕过网络问题**。
> 本次已额外备份到 `%TEMP%\pili_deps\`，供 `flutter clean` 后复用。

---

### 2.5 修复 CMake 安装前缀污染（导致产物不完整）

**现象**：构建"成功"，但 `build\windows\x64\runner\Debug\` 里**没有** `flutter_windows.dll` 和 `data\` 目录，exe 无法启动。

**根因**：第一次 CMake 配置在 mpv 校验处 `FATAL_ERROR` 中断，此时安装前缀尚未被设置，于是 `CMakeCache.txt` 里留下了默认值 `CMAKE_INSTALL_PREFIX = C:/Program Files/piliplus`；后续构建复用了这份被污染的缓存，Flutter 的安装步骤就把运行时文件拷贝到了 `C:\Program Files\piliplus`，而不是构建输出目录。

相关逻辑见 `windows/CMakeLists.txt`：

```cmake
set(BUILD_BUNDLE_DIR "$<TARGET_FILE_DIR:${BINARY_NAME}>")
if(CMAKE_INSTALL_PREFIX_INITIALIZED_TO_DEFAULT)      # ← 仅首次配置成立
  set(CMAKE_INSTALL_PREFIX "${BUILD_BUNDLE_DIR}" CACHE PATH "..." FORCE)
endif()
```

**处理**：彻底清除 `build` 目录（`flutter clean` + 结束占用进程），使 CMake 重新配置，安装前缀恢复为按配置解析的 `runner/<Config>`。

---

### 2.6 修复启动报错 `找不到 battery_plus_plugin.dll`

**现象**：双击运行时报系统错误 —— 由于找不到 `battery_plus_plugin.dll`，无法继续执行代码。

**排查**：`battery_plus_plugin.dll` 本身在目录中**存在**；对比 `C:\Program Files\piliplus`（2.5 产生的残缺点安装目录）与完整构建目录，发现前者**缺少 `WebView2Loader.dll`**。

**根因**：`flutter_inappwebview_windows` 插件因缺少 `WebView2Loader.dll` 初始化失败，连锁触发插件注册流程异常，Windows 最终把错误归因到错误链中的下一个插件名 `battery_plus_plugin.dll`。

**处理**：补入 `WebView2Loader.dll` 后即可启动。该缺陷仅存在于 2.5 那次残缺安装目录，正常构建输出目录始终完整。

**清理**：已删除 `C:\Program Files\piliplus` 残留（它与安装包默认目录 `C:\Program Files\PiliPlus` 大小写不敏感、会互相冲突）。

---

### 2.7 其他构建期问题

| 问题 | 原因 | 处理 |
| --- | --- | --- |
| `C1041` / `C1090`（PDB 报错） | 上一次中断构建残留的编译器进程锁定了 PDB | 结束 `mspdbsrv` / `cl` / `MSBuild` / `VBCSCompiler` 进程后重建 |
| `flutter clean` 无法删除 `build` | 目录被进程占用 | 结束相关进程后重新执行 `flutter clean` |
| 删除目录被安全守卫拦截 | 环境的批量删除保护 | 改用 `flutter clean` 等工具自身删除能力 |

---

## 3. 文件改动清单

| 文件 | 类型 | 说明 |
| --- | --- | --- |
| `pubspec.yaml` | 修改 | `flutter: 3.47.3` → `flutter: ">=3.47.3"` |
| `pubspec.lock` | 修改 | `pub get` 后自动更新 |
| `pili_release.json` | 新增 | 版本定义文件，供 `--dart-define-from-file` 使用（CI 中由 `build.ps1` 生成） |
| `D:\Source\flutter` | 修改 | 框架补丁，37 个文件 |
| `...\pub.flutter-io.cn\material_ui-1.2.0` | 修改 | 材质补丁，8 个补丁 |
| `D:\Tools\nuget\nuget.exe` | 新增 | NuGet CLI（SDK 外） |
| `build\windows\x64\*.7z` | 新增 | mpv / ANGLE 依赖包 |

`pili_release.json` 本次内容：

```json
{"pili.name":"2.1.3","pili.code":5344,"pili.time":1789275579,"pili.hash":"7c717237e2c6aaf51a5011436d1577601be44502"}
```

> `pili.name` 取自 `pubspec.yaml` 的版本号，`pili.code` 为 `git rev-list --count HEAD`，`pili.hash` / `pili.time` 为当前提交与构建时间。

---

## 4. 常用命令

### 4.1 首次配置（已在本机执行完毕）

```batch
flutter pub get
```

### 4.2 单独构建

```batch
set PATH=D:\Tools\nuget;%PATH%
cd /d D:\Downloads\Download\2\2\PiliPlus

:: 调试版
flutter build windows --debug --dart-define-from-file=pili_release.json

:: 发布版
flutter build windows --release --dart-define-from-file=pili_release.json
```

### 4.3 运行（推荐，可热重载）

```batch
set PATH=D:\Tools\nuget;%PATH%
cd /d D:\Downloads\Download\2\2\PiliPlus
flutter run -d windows
```

### 4.4 打包安装程序（CI 同款命令）

```batch
set PATH=D:\Tools\nuget;%LOCALAPPDATA%\Pub\Cache\bin;%PATH%
cd /d D:\Downloads\Download\2\2\PiliPlus
fastforge package --platform windows --targets exe ^
  --flutter-build-args="dart-define-from-file=pili_release.json,no-pub" --skip-clean
```

---

## 5. 构建产物

| 产物 | 路径 | 大小 |
| --- | --- | --- |
| 安装程序（Inno Setup，需管理员权限） | `dist\2.1.3+1\PiliPlus-2.1.3+1-windows-setup.exe` | 31.78 MB |
| 便携版压缩包 | `dist\PiliPlus-2.1.3-windows-x64-portable.zip` | 41.98 MB |
| 便携版解压目录（发布版） | `build\windows\x64\runner\Release\` | 26 个文件 / 25 个 DLL |
| 调试版运行目录 | `build\windows\x64\runner\Debug\` | — |

**Release 产物完整性校验**（均已确认存在）：
`piliplus.exe`、`flutter_windows.dll`、`WebView2Loader.dll`、`libmpv-2.dll`、`data\app.so`、`data\flutter_assets\`，以及 25 个插件 DLL。

**运行验证**：Release 版实测启动正常（内存约 223 MB，109 个已加载模块，窗口标题 `PiliPlus`）。

---

## 6. 注意事项

1. **补丁是必需的、也是全局的。** 项目依赖 Flutter SDK 内部实现，未打补丁无法编译；补丁会修改全局 `D:\Source\flutter` 与 pub 缓存中的 `material_ui`。升级 Flutter 后需重新应用补丁。
2. **构建前需确保 NuGet 在 PATH 中**，否则 `flutter_inappwebview_windows` 会配置失败。
3. **网络受限时需预置 mpv / ANGLE 压缩包**到 `build\windows\x64\`（已备份至 `%TEMP%\pili_deps\`，该目录可能被系统清理）。
4. **不要执行 `flutter clean` 后直接构建**，除非已把上述两个 `.7z` 放回 `build\windows\x64\`。
5. **安装包为 Inno Setup 生成的 `.exe`**（非 `.msi`），`PrivilegesRequired=admin`，默认安装到 `C:\Program Files\PiliPlus`，中英文双语界面。
6. `dist\`、`build\`、`pili_release.json` 均在 `.gitignore` 范围内，不会污染版本库。

---

## 7. 问题处理速查表

| 报错 / 现象 | 原因 | 解决 |
| --- | --- | --- |
| `flutter pub get` 版本解析失败 | SDK 精确版本约束不匹配 | 改为 `flutter: ">=3.47.3"` |
| 找不到 `TabBarState` 等框架内部类 | 未打 Flutter SDK 补丁 | 应用 `lib/scripts/*.patch` |
| CMake 需要 NuGet / WebView2 依赖 | 未安装 NuGet CLI | 安装 NuGet 并加入 PATH |
| 补丁 `git apply` 失败 | 补丁文件为 CRLF | 先转 LF 再应用 |
| `Integrity check failed, please try to re-build` | mpv / ANGLE 下载为 0 字节 | 用代理预下载并校验 MD5 |
| `C1041` / `C1090` | PDB 被残留进程锁定 | 结束 `mspdbsrv` / `cl` 等进程后重建 |
| 产物缺 `flutter_windows.dll`、`data\` | `CMakeCache.txt` 安装前缀被污染 | 清除 `build` 目录重新配置 |
| 找不到 `battery_plus_plugin.dll` | 残缺安装目录缺少 `WebView2Loader.dll` | 补全该 DLL / 使用完整产物 |
